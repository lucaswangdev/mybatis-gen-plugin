#macro(name $rawName)
${rawName.substring(0,1).toLowerCase()}${rawName.substring(1)}##
#end
package $model.repoPkg;

import com.lucaswangdev.common.BaseMapper;
import ${model.entityPkg}.${model.table.className}DO;
import ${model.paramPkg}.${model.table.className}Param;
import ${model.paramExtPkg}.${model.table.className}ParamExt;
import org.springframework.stereotype.Repository;

import java.util.List;

/**
 * ${model.table.className}MapperBase.
 * auto generated by MyBatis Generator Plugin, please do not modify it!
 */
@Repository
public interface ${model.table.className}Mapper extends BaseMapper<${model.table.className}Param, ${model.table.className}DO> {

    /**
     * 根据条件查询记录
     * @param param
     * @return
     */
    List<${model.table.className}DO> queryListByParamExt(${model.table.className}ParamExt param);

    /**
     * 根据条件查询记录数
     * @param param
     * @return
     */
    Long queryCountByParamExt(${model.table.className}ParamExt param);

    /* 以上部分由插件生成请勿修改 */
}